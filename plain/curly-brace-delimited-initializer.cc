#include <complex>
#include <iostream>
#include <vector>

int main(int argc, char * argv[]){

	double d {2.1};
	std::cout << "Double variable `d` is " << d << std::endl;

	std::complex<double> c {2.0, -1.1};
	std::cout << "Complex variable `c` is " << c << std::endl;

	std::vector<int> v {2, 3, -1};
	std::cout << "Sizeof(v) is " << sizeof(v) << std::endl;
	std::cout << "Sizeof(int) is " << sizeof(int) << std::endl;
	std::cout << "Count of v i s " << v. << std::endl;
	//std::cout << "Vector variable `v` is " << v << std::endl;
}

